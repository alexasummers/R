#defines a string variable myString, where we assign a string "Hello, world!"
myString <- "Hello, world!"
#Print() is being used to print the value stored in variable myString
print(myString)
#We can also save things using = rather than <- :
myString2 = "My first script in R"
print(myString2)

