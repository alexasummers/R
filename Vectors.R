#Single element vector
x = c(1.1)
print(x)

#Multiple elements vector
y <-c(1, 3, 2, 5)
print(y)

z <- c(4, 2, 3, 0)

#Check x, y and z length using length() function
length(x)
length(y)
length(z)

#Addition result of y + z

y + z

#Look at a list of all the objects that we have saved so far

ls()

#Delete object that we don't want
rn(x)

#Again, look at list of all objects we have saved so far
ls()